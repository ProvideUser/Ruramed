┌─────────────────────────────────────────────────────────────────┐
│                        APPLICATION STARTUP                       │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. MODULE IMPORTS & INITIALIZATION                             │
│     • dotenv.config() - Load environment variables             │
│     • Import database config, middleware, routes               │
│     • Set __dirname for ES modules                             │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. EXPRESS APP CONFIGURATION                                   │
│     • CORS: localhost:3000, localhost:3001 + credentials       │
│     • JSON/URL parsing: 10mb limit                             │
│     • Request logging (dev only)                               │
│     • Static files: /uploads → ./uploads directory             │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. ROUTE MOUNTING (ORDER MATTERS)                              │
│     /api/auth      → authRoutes                                 │
│     /api/medicines → medicineRoutes                             │
│     /api/users     → userRoutes                                 │
│     /api/addresses → addressRoutes                              │
│     /api/delivery  → deliveryRoutes                             │
│     /api/doctors   → doctorRoutes                               │
│     /api/orders    → orderRoutes                                │
│     /api/admin     → adminRoutes                                │
│     /api/geocode   → geocodingRoutes                            │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. SYSTEM ENDPOINTS                                            │
│     GET /api/health → System status & version info             │
│     GET /           → API documentation & endpoint map         │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. ERROR HANDLING (LAST IN CHAIN)                             │
│     • notFoundHandler → 404 for unmatched routes               │
│     • errorHandler → Global error processing                   │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. SERVER STARTUP SEQUENCE                                     │
│     • testConnection() → Verify database connectivity          │
│     • app.listen(PORT) → Start HTTP server                     │
│     • Console startup messages                                 │
│     • Process error handlers (uncaught exceptions)             │
│     • Graceful shutdown handlers (SIGTERM/SIGINT)              │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                       INCOMING REQUEST                         │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  MIDDLEWARE CHAIN (Sequential Processing)                       │
│                                                                 │
│  1. CORS Check                                                  │
│     ├─ Origin validation (localhost:3000/3001)                 │
│     └─ Credentials handling                                     │
│                                                                 │
│  2. Body Parsing                                                │
│     ├─ JSON parsing (10mb limit)                               │
│     └─ URL-encoded parsing (10mb limit)                        │
│                                                                 │
│  3. Request Logging (dev only)                                 │
│     └─ Log request details via requestLogger                   │
│                                                                 │
│  4. Static File Check                                           │
│     └─ /uploads/* → Serve from ./uploads directory             │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  ROUTE MATCHING (First Match Wins)                             │
│                                                                 │
│  /api/auth/*      → authRoutes                                  │
│  /api/medicines/* → medicineRoutes                              │
│  /api/users/*     → userRoutes                                  │
│  /api/addresses/* → addressRoutes                               │
│  /api/delivery/*  → deliveryRoutes                              │
│  /api/doctors/*   → doctorRoutes                                │
│  /api/orders/*    → orderRoutes                                 │
│  /api/admin/*     → adminRoutes                                 │
│  /api/geocode/*   → geocodingRoutes                             │
│  /api/health      → Health check                               │
│  /                → API documentation                          │
└─────────────────────────────────────────────────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   ▼                               ▼
        ┌─────────────────┐              ┌─────────────────┐
        │  ROUTE MATCHED  │              │  NO ROUTE MATCH │
        │  Process in     │              │  404 Handler    │
        │  Route Module   │              │  (next file)    │
        └─────────────────┘              └─────────────────┘
                   │                               │
                   └───────────────┬───────────────┘
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  ERROR HANDLING                                                 │
│     • Any thrown errors → errorHandler middleware              │
│     • Unmatched routes → notFoundHandler middleware            │
│     • Standardized JSON error responses                        │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE INITIALIZATION                      │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. ENVIRONMENT VARIABLE LOADING                                │
│     • dotenv.config() - Load .env file                         │
│     • DB_HOST     → MySQL server address                       │
│     • DB_USER     → Database username                          │
│     • DB_PASSWORD → Database password                          │
│     • DB_DATABASE → Target database name                       │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. CONNECTION POOL CREATION                                    │
│     mysql2/promise.createPool({                                 │
│       host: process.env.DB_HOST,                               │
│       user: process.env.DB_USER,                               │
│       password: process.env.DB_PASSWORD,                       │
│       database: process.env.DB_DATABASE,                       │
│       waitForConnections: true,     ← Queue when pool full     │
│       connectionLimit: 10,          ← Max 10 concurrent        │
│       queueLimit: 0                 ← Unlimited queue          │
│     })                                                          │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. CONNECTION POOL BEHAVIOR                                    │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  CONNECTION LIFECYCLE                                   │   │
│  │                                                         │   │
│  │  Request → Pool → Available Connection?                 │   │
│  │              │                                          │   │
│  │              ├─ YES → Use existing connection           │   │
│  │              │                                          │   │
│  │              └─ NO → Create new (if < 10 limit)        │   │
│  │                   │                                     │   │
│  │                   └─ Pool full → Queue request         │   │
│  │                                                         │   │
│  │  After Query → Connection returned to pool             │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. CONNECTION TESTING (testConnection Function)                │
│                                                                 │
│     try {                                                       │
│         await db.execute('SELECT 1');  ← Simple test query     │
│         console.log('✅ Database connected successfully');      │
│         return true;                                            │
│     } catch (error) {                                           │
│         console.error('❌ Database connection failed');         │
│         return false;                                           │
│     }                                                           │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    CONTROLLER MAKES DB CALL                    │
│                    (import { db } from config)                 │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  QUERY EXECUTION PATHWAY                                        │
│                                                                 │
│  db.execute(sql, params)                                        │
│        │                                                        │
│        ▼                                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  CONNECTION POOL MANAGER                                │   │
│  │                                                         │   │
│  │  1. Check available connections                         │   │
│  │     ├─ Available → Assign connection                    │   │
│  │     └─ None → Create new (up to limit: 10)             │   │
│  │                                                         │   │
│  │  2. Execute query on assigned connection               │   │
│  │     ├─ SQL: Prepared statement                         │   │
│  │     └─ Params: Sanitized parameters                    │   │
│  │                                                         │   │
│  │  3. Return connection to pool                          │   │
│  │     └─ Available for next query                        │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   ▼                               ▼
        ┌─────────────────┐              ┌─────────────────┐
        │  QUERY SUCCESS  │              │  QUERY FAILURE  │
        │  Return results │              │  Throw error    │
        │  to controller  │              │  to controller  │
        └─────────────────┘              └─────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│  POOL STATE MANAGEMENT                                          │
│                                                                 │
│  Active Connections: [■][■][■][□][□][□][□][□][□][□]            │
│                      └─ Used: 3, Available: 7                  │
│                                                                 │
│  Queued Requests:    [Request A][Request B][Request C]         │
│                      └─ Waiting for available connection       │
│                                                                 │
│  Connection Limits:                                             │
│  • Max Concurrent: 10 connections                              │
│  • Queue Limit: 0 (unlimited queuing)                          │
│  • Wait Behavior: true (queue requests vs reject)              │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST LIFECYCLE FLOW                      │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. REQUEST LOGGER (Development Only)                           │
│     • Log incoming request details                             │
│     • Start timer for response duration                        │
│     • Track: Method, URL, IP, User-Agent, Content-Type         │
│                                                                 │
│     📥 GET /api/medicines - IP: 127.0.0.1                      │
│     User-Agent: Mozilla/5.0...                                 │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. ROUTE PROCESSING                                            │
│     • Request flows through route handlers                     │
│     • Controllers execute business logic                       │
│     • Database operations performed                            │
└─────────────────────────────────────────────────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   ▼                               ▼
        ┌─────────────────┐              ┌─────────────────┐
        │  SUCCESS PATH   │              │   ERROR PATH    │
        │  Normal Response│              │  Exception      │
        └─────────────────┘              │  Thrown         │
                   │                     └─────────────────┘
                   ▼                               │
        ┌─────────────────┐                       ▼
        │  RESPONSE LOG   │              ┌─────────────────┐
        │  ✅ 200 (50ms)  │              │  ERROR HANDLER  │
        └─────────────────┘              │  (see below)    │
                                         └─────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLER ACTIVATION                     │
│                    (Any thrown error/exception)                 │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. ERROR LOGGING & CONTEXT CAPTURE                            │
│     console.error('❌ Global error handler:', {                 │
│       message: err.message,                                     │
│       stack: (development only),                                │
│       url: req.originalUrl,                                     │
│       method: req.method,                                       │
│       ip: req.ip,                                               │
│       userAgent: req.get('User-Agent')                          │
│     });                                                         │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. ERROR TYPE CLASSIFICATION & STATUS MAPPING                 │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  VALIDATION ERRORS                                      │   │
│  │  • ValidationError → 400 + field details               │   │
│  │  • Missing fields → 400 + required field list          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  AUTHENTICATION ERRORS                                  │   │
│  │  • JsonWebTokenError → 401 'Invalid token'             │   │
│  │  • TokenExpiredError → 401 'Token expired'             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  DATABASE ERRORS                                        │   │
│  │  • ER_DUP_ENTRY → 409 'Email/Phone already exists'     │   │
│  │  • ER_NO_REFERENCED_ROW_2 → 400 'Referenced not exist' │   │
│  │  • ER_ROW_IS_REFERENCED_2 → 400 'Cannot delete'        │   │
│  │  • ECONNREFUSED → 503 'Database connection failed'     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  REQUEST PARSING ERRORS                                 │   │
│  │  • entity.parse.failed → 400 'Invalid JSON format'     │   │
│  │  • entity.too.large → 413 'Request entity too large'   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. ERROR RESPONSE CONSTRUCTION                                 │
│     {                                                           │
│       error: "Standardized error message",                     │
│       status: 400|401|403|404|409|413|500|503,                │
│       message: "Human-readable description",                   │
│       timestamp: "2025-10-20T12:33:00.000Z",                  │
│       path: "/api/medicines",                                   │
│       method: "POST",                                           │
│       stack: "Error stack (development only)",                 │
│       details: "Additional error info (development only)"      │
│     }                                                           │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│  RATE LIMITING (createRateLimit)                               │
│                                                                 │
│  IP-based request tracking:                                     │
│  • Default: 100 requests per 15 minutes                        │
│  • Memory-based storage (Map)                                  │
│  • Sliding window cleanup                                       │
│  • 429 response when exceeded                                   │
│                                                                 │
│  Rate Limit Check:                                              │
│  Request → Check IP history → Within limit? → Continue         │
│                              → Exceeded? → 429 Error           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  VALIDATION (validateRequired)                                  │
│                                                                 │
│  Required field validation:                                     │
│  • Check req.body for specified fields                         │
│  • Verify non-empty values                                      │
│  • Return 400 with missing field list                          │
│                                                                 │
│  Validation Flow:                                               │
│  Fields Array → Check req.body → Missing? → 400 Error          │
│                                → Present? → Continue            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  ASYNC WRAPPER (asyncHandler)                                   │
│                                                                 │
│  Promise error handling:                                        │
│  • Wraps async route handlers                                   │
│  • Catches rejected promises                                    │
│  • Forwards errors to global error handler                     │
│                                                                 │
│  Usage: app.get('/route', asyncHandler(async (req, res) => {    │
│    // async operations that might throw                         │
│  }));                                                           │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE INITIALIZATION                    │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. MODULE SETUP                                                │
│     • Import JWT library for token verification                │
│     • Import mysql2/promise for database operations            │
│     • Load environment variables (JWT_SECRET, DB_*)            │
│     • Create separate database connection pool                 │
│       (Note: Duplicates config/database.js pool)               │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. EXPORTED MIDDLEWARE FUNCTIONS                               │
│     • authenticateToken() - JWT verification                   │
│     • isAdmin() - Role-based authorization                     │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                    PROTECTED ROUTE REQUEST                     │
│                    (Uses authenticateToken middleware)         │
└─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. TOKEN EXTRACTION                                            │
│     const authHeader = req.headers['authorization'];            │
│     const token = authHeader && authHeader.split(' ')[1];      │
│                                                                 │
│     Expected format: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6..."  │
│                            ▲                                    │
│                            └─ Extract this part                 │
└─────────────────────────────────────────────────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   ▼                               ▼
        ┌─────────────────┐              ┌─────────────────┐
        │  TOKEN PRESENT  │              │  NO TOKEN       │
        │  Continue       │              │  401 Response   │
        │  processing     │              │  "Access token  │
        └─────────────────┘              │   required"     │
                   │                     └─────────────────┘
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. JWT VERIFICATION                                            │
│     try {                                                       │
│         const decoded = jwt.verify(token, JWT_SECRET);          │
│         req.user = decoded;  ← Inject user data into request    │
│         next();             ← Continue to next middleware       │
│     } catch (error) {                                           │
│         return 403 "Invalid token"                              │
│     }                                                           │
└─────────────────────────────────────────────────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   ▼                               ▼
        ┌─────────────────┐              ┌─────────────────┐
        │  VALID TOKEN    │              │  INVALID TOKEN  │
        │  • Token verified│              │  • Expired      │
        │  • User data in │              │  • Malformed    │
        │    req.user     │              │  • Wrong secret │
        │  • Continue to  │              │  • 403 Response │
        │    route handler│              └─────────────────┘
        └─────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│  ROUTE PROTECTION PATTERNS                                      │
│                                                                 │
│  1. PUBLIC ROUTES (No middleware)                               │
│     app.get('/api/medicines', controller.getMedicines);        │
│     ↳ No authentication required                               │
│                                                                 │
│  2. PROTECTED ROUTES (Authentication required)                 │
│     app.get('/api/profile', authenticateToken,                 │
│                            controller.getProfile);             │
│     ↳ Must have valid JWT token                                │
│                                                                 │
│  3. ADMIN ROUTES (Authentication + Admin role)                 │
│     app.post('/api/admin/users', authenticateToken,            │
│                                  isAdmin,                      │
│                                  controller.createUser);       │
│     ↳ Must have valid JWT + admin role                         │
│                                                                 │
│  4. CHAINED MIDDLEWARE EXECUTION                                │
│     Request → authenticateToken → isAdmin → Controller         │
│               ↓                   ↓         ↓                  │
│               401/403            403        Business Logic     │
│               if no token        if not     if authorized      │
│                                  admin                         │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│  JWT TOKEN PAYLOAD (req.user after verification)               │
│                                                                 │
│  Expected decoded structure:                                    │
│  {                                                              │
│    id: 123,                    ← User ID                       │
│    email: "user@example.com",  ← User email                    │
│    role: "admin|user|doctor",  ← User role for authorization   │
│    iat: 1640995200,           ← Issued at timestamp            │
│    exp: 1641081600            ← Expiration timestamp           │
│  }                                                              │
│                                                                 │
│  This data becomes available as req.user in controllers        │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│  AUTHENTICATION ERROR RESPONSES                                │
│                                                                 │
│  401 Unauthorized (No Token):                                  │
│  {                                                              │
│    "error": "Access token required"                            │
│  }                                                              │
│                                                                 │
│  403 Forbidden (Invalid Token):                                │
│  {                                                              │
│    "error": "Invalid token"                                    │
│  }                                                              │
│                                                                 │
│  403 Forbidden (Insufficient Role):                            │
│  {                                                              │
│    "error": "Admin access required"                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘















# PROMPT

Create a frontend ap for this backend. The details of app are below:

We are creating a website integrating the backend to this :
 - Homepage:
    - Header (left - clickable logo to homepage, location button near logo, profile icon)
       - Location icon(On click)
         - Opens a dropdown/popover to search and select location(dynamic searchbar, no search button)
         - below search bar, saved addresses of user from database.
       - Profile icon:
         - Dashboard
         - logout
    - Hero section with welcome elements
    - below welcome elements in hero section login based button(dashboard/get started) and order medicine button
    - below hero section, few more sections explaining the websites features, etc
    - footer with logo, links, contact info, social media, and T&C privacy policy etc

  - Dashboard(below will be shown in sidebar layout):
    - Profile information
    - Addresses 
    - Doctor consultation Appointment 
    - Buy medicine with prescription
    - My Orders

Note : This flow will be for Users and not Admin
Admin flow will be logged in through another PORT/ Address


# AI PROMPT

create a new frontend folder for this 

Goal:
Create a responsive, modern frontend web application for a medical services platform that integrates with an existing backend. The frontend should follow the structure, flow, and UI/UX behavior described below.

Create a mobile-first, visually engaging, and accessible design theme for a healthcare-oriented web application. The design should emphasize clarity, trust, and smooth interaction.

🔹 Design Style & Aesthetics

Mobile-first approach:

Layouts and components should be designed primarily for mobile screens and gracefully scale up to tablets and desktops.

Ensure touch-friendly elements, larger tap targets, and vertical flow optimized for small screens.

Color Palette:

Light Theme: Whites, soft blues, greens, and light grays for a clean, medical, trustworthy appearance.

Dark Theme: Deep navy or charcoal background with soft accent colors (teal, mint, or light blue) for readability and comfort.

Maintain high contrast and accessibility standards in both modes.

Typography:

Modern, sans-serif font (e.g., Inter, Poppins, or Lato).

Consistent font sizing scale (responsive typography).

Clear hierarchy with distinct heading, subheading, and body text styles.

Icons & Illustrations:

Use line or duotone medical/healthcare-style icons (consistent stroke width).

Minimal and friendly illustrations that complement the content without clutter.

🔹 Transitions & Visual Behavior

Implement smooth, subtle transitions throughout the UI:

Page and section transitions should fade or slide softly.

Hover and click animations (buttons, links) should have gentle scaling or shadow effects.

Dropdowns, modals, and popovers open with a short easing animation (e.g., ease-in-out, 150–200ms).

Use consistent motion curves across the interface for harmony and polish.

Prioritize fluid micro-interactions:

Buttons: soft color transitions and shadow depth changes on hover/press.

Cards and tiles: light hover elevation or background tint.

Input fields: smooth focus transitions with border or glow highlight.

🔹 Dark / Light Theme Toggle

Add a theme toggle button in the header:

Position: top-right area (beside profile or login button).

Icon-based toggle (🌞 / 🌙) with animation between states.

Persist user preference using local storage or system color scheme detection.

All UI elements (buttons, backgrounds, text, icons) must adjust dynamically to theme changes with smooth color transitions (e.g., 300ms fade).

🔹 Accessibility & Consistency

Follow WCAG AA or higher standards for contrast.

Ensure all icons and images have descriptive alt text.

Maintain consistent spacing, border-radius, and shadow patterns throughout the design.

🔹 Overall Mood

Clean, trustworthy, and modern healthcare aesthetic.

Feels calm, professional, and friendly — not overly clinical.

Visual cues should promote user confidence and comfort during interactions.

🔹 General Info

Type: User-facing Web Application (Admin has a separate login on another port/address)

Framework Preference: (Specify React, Next.js, or Vue.js)

Backend: Already built; frontend should connect via REST or GraphQL APIs

Authentication: Token/session-based (JWT or cookies)


🔹 Pages & Components
1. Homepage
Header

Left Section:

Clickable Logo: Redirects to homepage (/).

Location Button/Icon:

On click → Opens a dropdown or popover containing:

Dynamic Search Bar: Real-time location search (auto-suggest, no “search” button).

Saved Addresses: List of user’s saved addresses fetched from backend.

Right Section:

If user is logged in:

Profile Icon → On click, open dropdown menu with:

“Dashboard” (navigates to /dashboard)

“Logout” (calls backend logout API and redirects to homepage)

If user is NOT logged in:

Show Login / Signup button instead of the profile icon.

Clicking it opens a modal or redirects to authentication page (/login or /signup).

Hero Section

Prominent welcome heading (e.g., “Welcome to [Website Name] — Your Trusted Healthcare Partner”)

Supporting tagline/subtext introducing the platform’s mission and convenience.

Below the welcome content:

Conditional CTA Button:

If logged in → “Go to Dashboard”

If not logged in → “Get Started” (redirects to login/signup)

Order Medicine Button: Always visible, redirects to the medicine ordering flow or section.

Feature Sections (Below Hero)

Series of visually engaging cards or blocks highlighting the platform’s main features:

“Consult Top Doctors Online”

“Order Medicines from Verified Pharmacies”

“Track Health Records & Orders Easily”

Each section includes an icon/illustration, short description, and light animation or hover effect.

Footer

Includes:

Website logo

Navigation links (Home, About, Contact, Dashboard)

Contact info (email, phone)

Social media icons

Legal links: Terms & Conditions, Privacy Policy

2. Dashboard

Layout: Sidebar + Main Content area
Sidebar is persistent and collapsible on mobile screens.

Sidebar Items:

Profile Information

Addresses

Doctor Consultation / Appointments

Buy Medicine (Prescription Upload)

My Orders

Dashboard Section Behaviors:

Profile Information:

Display and allow editing of user data (name, email, phone, etc.)

Addresses:

Manage saved addresses (add, edit, delete)

Doctor Consultation / Appointment:

Browse doctors, view available slots, book appointments.

Buy Medicine:

Upload prescriptions, browse available medicines, place orders.

My Orders:

View order history, track ongoing orders, reorder past medicines.

All dashboard sections should update dynamically (SPA style, no full-page reloads).

🔹 Functional Details

User Flow:

Unauthenticated users → can view homepage, but must log in/sign up to access dashboard and personalized features.

Authenticated users → access dashboard, saved addresses, orders, and more.

Responsive Design:

Fully optimized for desktop, tablet, and mobile layouts.

Sidebar collapses into a hamburger menu on smaller screens.

State Management:

Use React Context, Redux, or Zustand for global state (auth, user info, addresses, etc.).

Routing:

/ → Homepage

/login or /signup → Auth pages

/dashboard → Dashboard landing

/dashboard/[section] → Nested dashboard sections

API Integration:

Integrate all dynamic actions (login, logout, location search, addresses, appointments, orders) with backend endpoints.

✅ Deliverables Expected

Full frontend codebase following modern best practices.

Component-based architecture:

Header

Footer

Hero Section

Sidebar

Dynamic Dashboard Components

Auth state handling (login/logout UI conditions).

Responsive layout and clean design.

Clearly marked API integration points (fetch, axios, or equivalent).

Routing setup for SPA navigation.